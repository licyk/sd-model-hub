# CLAUDE.md

See [AGENTS.md](AGENTS.md). It holds the project description, the layout and layer rule, the
conventions, the specifications for detection, the library, downloads, sources, authentication,
the API and the web UI, the testing approach, the invariants to keep, and the known gaps.

Keep AGENTS.md as the single source: when something durable changes, update it there rather than
repeating it here.

Before calling a change done, run:

```bash
python scripts/dev.py check
```
